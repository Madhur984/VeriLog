/** Shared animation data-* props stamped onto root &lt;g&gt; for animation modules */

/** Shared animation data-* props stamped onto root <g> for animation modules */
export interface AnimProps {
    dataCompId?: string;
    dataType?: string;
    dataConnected?: boolean;
    dataDragging?: boolean;
    dataBaseX?: number;
    dataBaseY?: number;
}

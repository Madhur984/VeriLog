node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.production

# Local MCP config (contains Hostinger API token)
.mcp.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Gemini settings (track config, not secrets)
!.gemini/settings.json

# Study Materials
study_material/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.venv/
venv/
ENV/

# Blender backup files
*.blend1

# Claude agent configuration directory
.claude/

# Generated audit reports
reports/

# Local scratch and source assets
_scratch/
src/be/

# Yosys WASM engine is served from node_modules at /yowasp/* in dev and copied
# into dist/yowasp on build by the bfb-yowasp-assets Vite plugin — nothing to
# commit (dist/ is already ignored; node_modules holds the pinned source).

# Standalone "Coming Soon" waitlist page — deployed separately to Hostinger,
# not part of the React app.
bitforbytes-waitlist/
bitforbytes-waitlist.zip
bitforbytes-waitlist-deploy.zip


